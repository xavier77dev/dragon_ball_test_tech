{
  "buildCommand": "npm install && npm run build",
  "outputDirectory": "/dist",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/backend/(.*)",
      "destination": "backend/src/app.ts"
    }
  ]
}
